---
phase: 01-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AUDIT-REPORT.md
autonomous: false
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05

must_haves:
  truths:
    - "User can open AUDIT-REPORT.md and see every script classified as Keep, Remove, or Review with a reason"
    - "User can see a consolidated table of YouTuber-specific tools found across all config files"
    - "User can see all env vars and aliases that reference undefined or personal paths"
    - "User can read a list of missing tools with install method and priority"
    - "User can read concrete DevPod improvement suggestions"
  artifacts:
    - path: "AUDIT-REPORT.md"
      provides: "Complete audit report with 5 sections"
      contains: "## Scripts"
  key_links: []
---

<objective>
Read every file in the dotfiles repo and produce AUDIT-REPORT.md at the repo root, classifying all scripts, flagging YouTuber-specific tools across all configs, identifying personal/missing env vars and aliases, listing missing tools, and providing concrete DevPod improvement suggestions.

Purpose: Give the user a clear, actionable picture of the inherited dotfiles so Phase 2 (Cleanup) can proceed with confidence.
Output: AUDIT-REPORT.md at repo root with 5 sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit/1-CONTEXT.md
@.planning/phases/01-audit/1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all repo files and write AUDIT-REPORT.md</name>
  <files>AUDIT-REPORT.md</files>
  <read_first>
    .planning/phases/01-audit/1-CONTEXT.md
    .planning/phases/01-audit/1-RESEARCH.md
    scripts/0-cd
    scripts/backup
    scripts/backup-weekly
    scripts/big
    scripts/bulkappend
    scripts/bulkreplace
    scripts/cantsleep
    scripts/center
    scripts/curr
    scripts/day_bash
    scripts/delrg
    scripts/dnd
    scripts/dndstatus
    scripts/duck
    scripts/focusstart
    scripts/focusstop
    scripts/gendate
    scripts/goentr
    scripts/goentrtest
    scripts/google
    scripts/hellobash
    scripts/iterator
    scripts/jqedit
    scripts/m
    scripts/multiedit
    scripts/nb
    scripts/newscript
    scripts/parameter_parsing
    scripts/path
    scripts/pomocalc
    scripts/postgres-db-list
    scripts/present
    scripts/pub
    scripts/push
    scripts/reprename
    scripts/scandisks
    scripts/shorts
    scripts/small
    scripts/startbreak
    scripts/stop.sh
    scripts/sunrise
    scripts/ticket
    scripts/transcode-audio
    scripts/urlencode
    scripts/week
    scripts/welcome
    scripts/ytr
    dot_zshrc
    dot_zprofile
    dot_tmux.conf
    dot_config/mise/config.toml
    setup
  </read_first>
  <action>
Read every script in scripts/ (all 46) and every config file listed in read_first. Then write AUDIT-REPORT.md at the repo root with the following exact structure and content. Use the research in 1-RESEARCH.md as a starting reference but VERIFY every classification by reading the actual files.

**CRITICAL: Apply these locked classification rules from CONTEXT.md decisions:**
- Per D-04: Three buckets only: Keep, Remove, Review
- Per D-05: Scripts with hardcoded `/users/mischa/` paths -> Remove
- Per D-06: Scripts using tools not in containers (pomo, newsboat, brightnessctl, osascript) -> Remove
- Per D-07: Generic shell utilities (urlencode, gendate, m, push, reprename, bulkreplace, jqedit) -> Keep
- Per D-08: Flag YouTuber tools across ALL files (zshrc, mise, setup, tmux, scripts)
- Per D-09: Known YouTuber tools: pomo, newsboat, brightnessctl, osascript, vivaldi, lazygit (if not in mise), pass, dnd (macOS-only)
- Per D-10: K8s tools (kubectl, flux, k3d) are Keep -- user has K8s homelab
- Per D-11: Flag vars: $ICLOUD, $ZETTELKASTEN, $LAB, $REPOS (verify), GITUSER
- Per D-12: Flag aliases: cdblog, in, cdzk, cdgo, homelab, hl (homelab is Keep -- user has K8s homelab)
- Per D-13: GITUSER="santiagobermudezparra" is user's own -- Keep but note hardcoded
- Per D-14: Suggest Node (note conflict: already in mise, user wants via setup.sh), Claude Code CLI
- Per D-15: Suggest gh CLI, lazygit, direnv, delta
- Per D-16: Do NOT suggest Python in global mise config
- Per D-17: Suggestions must be concrete and actionable ("add X by doing Y")
- Per D-18: Include DevPod readiness section

**Exact file structure for AUDIT-REPORT.md:**

```markdown
# Audit Report

## Scripts

| Name | Classification | Reason | Notes |
|------|----------------|--------|-------|
```

Include all 46 scripts from scripts/ directory. Each row must have the script name (without `scripts/` prefix), one of Keep/Remove/Review, a brief reason for the classification, and any relevant notes.

Per D-01: File goes at repo root (not in .planning/).
Per D-02: Sections in this order: Scripts, YouTuber-Specific Tools, Env Vars and Aliases, Missing Tools, DevPod Suggestions.
Per D-03: Each section uses a table.

```markdown
## YouTuber-Specific Tools

| Tool | Found In | Classification | Notes |
|------|----------|----------------|-------|
```

Per D-08: Search ALL files for YouTuber tool references. Include: pomo (scripts + tmux.conf status-right), newsboat (scripts/nb), brightnessctl (scripts/cantsleep), osascript (scripts/dnd, scripts/dndstatus), vivaldi (dot_zshrc conditional), pass (dot_zshrc alias `pc`), fabric (scripts/ytr), wl-copy (scripts/ytr), figlet (scripts/startbreak), gsed (scripts/big, small, multiedit, ticket), w3m (scripts/duck, google), az CLI (dot_zshrc alias `sub`), entr (scripts/goentr, goentrtest -- Keep). Per D-09 and D-10: K8s tools are Keep.

```markdown
## Env Vars & Aliases

| Item | Type | Status | Notes |
|------|------|--------|-------|
```

Per D-11: Flag $ICLOUD (undefined), $ZETTELKASTEN (undefined), $LAB (undefined). Check $REPOS -- it IS defined as `$HOME/Repos` so mark Keep.
Per D-13: GITUSER="santiagobermudezparra" is Keep, note hardcoded.
Per D-12: cdblog (Remove -- Mischa's blog), in (Remove -- $ZETTELKASTEN undefined), cdzk (Remove -- $ZETTELKASTEN undefined), cdgo (Review -- generic pattern but assumes Go repos), homelab (Keep -- user has K8s homelab), hl (Keep -- shorthand for homelab). Also include: icloud alias (Remove -- $ICLOUD undefined), lab alias (Remove -- $LAB undefined), sub alias (Remove -- Azure CLI, Mischa's client), pc alias (Remove -- pass, Mischa's password manager), 0 alias (Review -- unknown directory purpose), zo alias (Review -- depends on 0-cd script), syu alias (Review -- Arch-specific), BROWSER=vivaldi (Remove -- not in containers).

Status column values: Keep, Remove, or Review.

```markdown
## Missing Tools

| Tool | Purpose | Install Method | Priority |
|------|---------|----------------|----------|
```

Per D-14: Node (note: already in mise global config -- conflict with user wanting it via setup.sh; flag for Phase 3 reconciliation), Claude Code CLI (install via `npm install -g @anthropic-ai/claude-code` in setup.sh after Node).
Per D-15: gh (GitHub CLI -- add to mise: `gh = "latest"`), lazygit (add to mise: `lazygit = "latest"` -- alias already exists in zshrc), direnv (add to mise: `direnv = "latest"` -- hook already in zshrc), delta (add to mise: `delta = "latest"` -- better git diff).
Per D-16: Do NOT list Python.

Priority values: High (user explicitly wants), Medium (suggested useful addition).

```markdown
## DevPod Suggestions
```

Per D-17 and D-18: Write a numbered list of concrete, actionable improvements. Include:

1. Guard `sudo chsh` in setup.sh with a container check (e.g., `if [ -z "$DEVCONTAINER_TYPE" ]; then sudo chsh ...; fi`) -- currently may fail in containers without sudo
2. Remove or gate alacritty-theme clone in setup.sh behind `command -v alacritty` (already partially gated but could be cleaner)
3. Add `mkdir -p "$HOME/Repos"` to setup.sh -- zshrc assumes `$REPOS=$HOME/Repos` exists
4. Remove `set -g status-right "#(pomo)"` from dot_tmux.conf or replace with a generic status (pomo binary not available)
5. Remove vivaldi browser detection block from dot_zshrc -- irrelevant in containers
6. Add Node and Claude Code CLI installation to setup.sh (per D-14)
7. Consider adding gh, lazygit, direnv, delta to mise config.toml for a richer container dev experience (per D-15)
8. Remove or comment out desktop-only config directories (alacritty/, hypr/, waybar/) from chezmoi management when targeting containers, or gate with chezmoi templates
9. Reconcile Node presence in both mise global config and setup.sh (currently in mise -- user wants it in setup.sh per D-14)
10. Create a `.privaterc` template or document that it needs to exist -- zshrc sources `$HOME/.privaterc` at the end
  </action>
  <verify>
    <automated>test -f AUDIT-REPORT.md && grep -c "^|" AUDIT-REPORT.md | xargs test 60 -le</automated>
  </verify>
  <acceptance_criteria>
    - File exists at repo root: `test -f AUDIT-REPORT.md` returns 0
    - Contains Scripts section: `grep -q "## Scripts" AUDIT-REPORT.md`
    - Contains YouTuber-Specific Tools section: `grep -q "## YouTuber-Specific Tools" AUDIT-REPORT.md`
    - Contains Env Vars section: `grep -q "## Env Vars" AUDIT-REPORT.md`
    - Contains Missing Tools section: `grep -q "## Missing Tools" AUDIT-REPORT.md`
    - Contains DevPod Suggestions section: `grep -q "## DevPod Suggestions" AUDIT-REPORT.md`
    - All 46 scripts appear: `grep -c "^|" AUDIT-REPORT.md` is at least 60 (46 scripts + tool rows + env var rows + missing tools rows)
    - Classification buckets used: `grep -cE "\bKeep\b|\bRemove\b|\bReview\b" AUDIT-REPORT.md` is at least 46
    - Key Remove scripts present: `grep -q "backup.*Remove" AUDIT-REPORT.md && grep -q "pub.*Remove" AUDIT-REPORT.md && grep -q "present.*Remove" AUDIT-REPORT.md`
    - Key Keep scripts present: `grep -q "bulkreplace.*Keep" AUDIT-REPORT.md && grep -q "urlencode.*Keep" AUDIT-REPORT.md && grep -q "jqedit.*Keep" AUDIT-REPORT.md`
    - GITUSER noted as Keep: `grep -q "GITUSER.*Keep" AUDIT-REPORT.md`
    - Node conflict flagged: `grep -qi "node.*mise\|mise.*node" AUDIT-REPORT.md`
    - pomo flagged in tmux: `grep -q "pomo.*tmux\|tmux.*pomo" AUDIT-REPORT.md`
    - Claude Code CLI suggested: `grep -qi "claude.*code\|claude-code" AUDIT-REPORT.md`
    - No Python suggestion in global mise: output of `grep -i "python.*mise.*global\|add python.*mise" AUDIT-REPORT.md` is empty
  </acceptance_criteria>
  <done>AUDIT-REPORT.md exists at repo root with all 5 sections, all 46 scripts classified, YouTuber tools flagged across all configs, env vars and aliases classified, missing tools listed with install methods, and concrete DevPod suggestions provided.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews AUDIT-REPORT.md</name>
  <files>AUDIT-REPORT.md</files>
  <read_first>AUDIT-REPORT.md</read_first>
  <action>Present AUDIT-REPORT.md to the user for review. No automated changes -- this is a human verification gate. The user confirms the audit is accurate or requests corrections before Phase 2 proceeds.</action>
  <what-built>Complete AUDIT-REPORT.md classifying all scripts, tools, env vars, aliases, and providing DevPod improvement suggestions.</what-built>
  <how-to-verify>
    1. Open AUDIT-REPORT.md at the repo root
    2. Check Scripts section: verify all 46 scripts are listed with sensible classifications
    3. Check YouTuber-Specific Tools section: verify pomo, newsboat, brightnessctl, osascript, vivaldi, pass are flagged
    4. Check Env Vars and Aliases section: verify $ICLOUD, $ZETTELKASTEN, $LAB flagged as Remove; GITUSER flagged as Keep (hardcoded); K8s aliases (k, kgp, fgk, homelab) flagged as Keep
    5. Check Missing Tools section: verify Node conflict noted, Claude Code CLI listed, gh/lazygit/direnv/delta suggested
    6. Check DevPod Suggestions: verify concrete actionable items (not vague)
    7. Flag any "Review" items you want reclassified before Phase 2
  </how-to-verify>
  <verify>User confirms report accuracy by typing "approved"</verify>
  <acceptance_criteria>
    - User has reviewed all 5 sections of AUDIT-REPORT.md
    - User has confirmed or corrected any "Review" classified items
    - User has typed "approved" or corrections have been applied
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any corrections needed</resume-signal>
  <done>User has reviewed and approved AUDIT-REPORT.md, confirming it is accurate and complete for Phase 2 to proceed.</done>
</task>

</tasks>

<verification>
- AUDIT-REPORT.md exists at repo root (per D-01)
- Report has 5 sections in order: Scripts, YouTuber-Specific Tools, Env Vars and Aliases, Missing Tools, DevPod Suggestions (per D-02)
- Each section uses appropriate tables (per D-03)
- All 46 scripts classified using Keep/Remove/Review buckets (per D-04)
- Classification rules D-05 through D-07 correctly applied
- YouTuber tools flagged across ALL config files, not just scripts/ (per D-08)
- K8s tools marked as Keep (per D-10)
- No deferred work included (no file deletions, no setup.sh changes, no Ralph integration)
</verification>

<success_criteria>
User can open AUDIT-REPORT.md and find:
1. Every script in scripts/ classified with a reason (AUDIT-01)
2. A consolidated list of YouTuber-specific tools across all configs (AUDIT-02)
3. All personal/undefined env vars and aliases identified (AUDIT-03)
4. Missing tools with concrete install suggestions (AUDIT-04)
5. Actionable DevPod improvement suggestions (AUDIT-05)
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-01-SUMMARY.md`
</output>
