---
phase: 02-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dot_zshrc
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "No alias references undefined variables (ICLOUD, ZETTELKASTEN, LAB)"
    - "No YouTuber-specific or personal aliases remain (cdblog, sub, pc)"
    - "No orphaned section headers remain (sections whose aliases were all removed)"
    - "All K8s aliases, homelab aliases, generic aliases, and env vars are intact"
    - "Vivaldi browser detection block is removed"
  artifacts:
    - path: "dot_zshrc"
      provides: "Clean zshrc with only relevant aliases and env vars"
  key_links:
    - from: "dot_zshrc"
      to: "shell environment"
      via: "aliases and exports"
      pattern: "alias (k|kgp|kc|kn|fgk|homelab|hl|dot|repos|ghrepos|gr|ds)="
---

<objective>
Remove all unused aliases, undefined env var references, and the Vivaldi browser block from dot_zshrc.

Purpose: Eliminate aliases that silently fail (undefined vars), personal/client-specific aliases (Azure, pass, blog), and irrelevant browser detection so the zshrc is clean for DevPod use.
Output: A dot_zshrc with only working, relevant aliases and env vars.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AUDIT-REPORT.md
@dot_zshrc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead aliases, env vars, and section headers from dot_zshrc</name>
  <files>dot_zshrc</files>
  <read_first>dot_zshrc (full file — 263 lines), AUDIT-REPORT.md (Env Vars and Aliases table — every row with Status=Remove)</read_first>
  <action>
Edit `dot_zshrc` to make the following exact removals. Use line numbers from the current file for reference but match on exact content to avoid off-by-one errors.

**1. Remove Vivaldi browser block (lines 31-33):**
Delete these 3 lines entirely:
```
if command -v vivaldi > /dev/null ; then
  export BROWSER="vivaldi"
fi
```

**2. Remove alias cdblog (line 138):**
Delete: `alias cdblog="cd ~/websites/blog"`

**3. Remove alias icloud (line 140):**
Delete: `alias icloud="cd \$ICLOUD"`

**4. Remove the "# 0" section block (lines 151-154):**
Delete these lines (the blank line before, the comment, and both aliases):
```

# 0

alias 0='cd $HOME/0'
alias zo='eval "$($SCRIPTS/0-cd)"'
```

**5. Remove alias lab (line 158):**
Delete: `alias lab='cd $LAB'`
This is inside the "# Repos" section. Keep the "# Repos" header and the other aliases in that section (dot, repos, ghrepos, gr, cdgo).

**6. Remove the "# Azure" section block (lines 178-180):**
Delete these lines (the comment and the alias):
```
# Azure

alias sub='az account set -s'
```

**7. Remove the "# Zettelkasten" section block (lines 189-192):**
Delete these lines (the blank line, comment, and both aliases):
```

# Zettelkasten

alias in="cd \$ZETTELKASTEN/0\ Inbox/"
alias cdzk="cd \$ZETTELKASTEN"
```

**8. Remove the "# Pass" section block (lines 205-207):**
Delete these lines (the comment and the alias):
```
# Pass

alias pc='pass show -c'
```

**KEEP all of the following (do NOT remove):**
- All env vars: REPOS, GITUSER, GHREPOS, DOTFILES, SCRIPTS, GOBIN, GOPRIVATE, GOPATH, EDITOR, LANG
- All generic aliases: v, cat, scripts, c, ls/ll/la/lla/lt, dot, repos, ghrepos, gr, lastmod, t, e, gp, gs, lg
- K8s aliases: k, kgp, kc, kn, fgk
- Homelab aliases: homelab, hl
- DevPod alias: ds
- Review aliases (leave in place): cdgo, syu
- SSH/GPG block, Mise block, Path block, History, Prompt, Completion, Sourcing sections — all untouched

After all removals, clean up any resulting double-blank-line gaps (max one blank line between sections).
  </action>
  <verify>
    <automated>! grep -q 'alias cdblog\|alias icloud\|alias 0=\|alias zo=\|alias lab=\|alias sub=\|alias in=\|alias cdzk\|alias pc=' dot_zshrc && ! grep -q 'vivaldi' dot_zshrc && ! grep -q '# Azure' dot_zshrc && ! grep -q '# Zettelkasten' dot_zshrc && ! grep -q '# Pass' dot_zshrc && ! grep -q '# 0' dot_zshrc && echo "PASS: all removals confirmed" || echo "FAIL: some items still present"</automated>
  </verify>
  <acceptance_criteria>
- `grep 'alias cdblog' dot_zshrc` returns no match (exit 1)
- `grep 'alias icloud' dot_zshrc` returns no match
- `grep "alias 0=" dot_zshrc` returns no match
- `grep 'alias zo=' dot_zshrc` returns no match
- `grep 'alias lab=' dot_zshrc` returns no match
- `grep 'alias sub=' dot_zshrc` returns no match
- `grep 'alias in=' dot_zshrc` returns no match
- `grep 'alias cdzk' dot_zshrc` returns no match
- `grep 'alias pc=' dot_zshrc` returns no match
- `grep 'vivaldi' dot_zshrc` returns no match
- `grep '# Azure' dot_zshrc` returns no match
- `grep '# Zettelkasten' dot_zshrc` returns no match
- `grep '# Pass' dot_zshrc` returns no match
- `grep '# 0' dot_zshrc` returns no match (the section header, not the number 0 elsewhere)
- KEPT aliases are still present: `grep -c 'alias k=\|alias kgp=\|alias homelab=\|alias hl=\|alias ds=\|alias dot=\|alias repos=\|alias ghrepos=' dot_zshrc` returns 8
- KEPT env vars still present: `grep -c 'REPOS\|GITUSER\|GHREPOS\|DOTFILES\|SCRIPTS\|GOBIN\|GOPRIVATE\|GOPATH' dot_zshrc` returns 8 or more
- Review aliases still present: `grep 'alias cdgo\|alias syu' dot_zshrc` returns 2 matches
- No triple-or-more blank lines: `awk '/^$/{c++;if(c>2)exit 1}/./{c=0}' dot_zshrc` exits 0
  </acceptance_criteria>
  <done>dot_zshrc contains only working, relevant aliases and env vars. All Remove items deleted. All Keep and Review items intact. No orphaned section headers. No excessive blank lines.</done>
</task>

</tasks>

<verification>
- `grep -c 'ICLOUD\|ZETTELKASTEN\|LAB' dot_zshrc` returns 0 (no references to undefined vars)
- `grep -c 'vivaldi\|cdblog\|pass show\|az account\|0-cd' dot_zshrc` returns 0
- `grep 'alias k=' dot_zshrc` returns a match (K8s aliases intact)
- `grep 'alias ds=' dot_zshrc` returns a match (DevPod alias intact)
- `grep 'DOTFILES\|SCRIPTS\|REPOS\|GITUSER' dot_zshrc` returns matches (env vars intact)
- `grep 'alias cdgo\|alias syu' dot_zshrc` returns 2 matches (Review items untouched)
</verification>

<success_criteria>
1. Zero aliases referencing undefined variables (ICLOUD, ZETTELKASTEN, LAB) remain
2. Zero YouTuber/personal aliases (cdblog, sub, pc, 0, zo) remain
3. Zero orphaned section headers (# 0, # Azure, # Zettelkasten, # Pass) remain
4. Vivaldi browser detection block removed
5. All K8s, homelab, generic, and DevPod aliases intact
6. All legitimate env vars intact
7. Review aliases (cdgo, syu) left in place
</success_criteria>

<output>
After completion, create `.planning/phases/02-cleanup/02-02-SUMMARY.md`
</output>
