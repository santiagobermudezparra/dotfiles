---
phase: 03-ralph-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ralph
  - dot_claude/CLAUDE.md
  - dot_claude/skills/ralph/SKILL.md
  - dot_claude/skills/prd/SKILL.md
  - dot_config/claude/CLAUDE.md
autonomous: true
requirements:
  - RALPH-01
  - RALPH-03
  - RALPH-04

must_haves:
  truths:
    - "User can run `ralph` from any project directory (script is on PATH via $SCRIPTS)"
    - "Claude Code auto-reads ~/.claude/CLAUDE.md on every session start"
    - "CLAUDE.md contains Ralph conventions (10-step workflow, progress reporting, quality gates, stop condition)"
    - "Skills for PRD generation and PRD-to-JSON conversion are available globally"
    - "The wrong-path dot_config/claude/CLAUDE.md no longer exists"
  artifacts:
    - path: "scripts/ralph"
      provides: "Ralph agent loop script callable as `ralph`"
      contains: "Ralph Wiggum"
    - path: "dot_claude/CLAUDE.md"
      provides: "Global Claude Code instructions with Ralph conventions"
      contains: "prd.json"
    - path: "dot_claude/skills/ralph/SKILL.md"
      provides: "Ralph PRD converter skill"
      contains: "prd.json"
    - path: "dot_claude/skills/prd/SKILL.md"
      provides: "PRD generator skill"
      contains: "prd-"
  key_links:
    - from: "scripts/ralph"
      to: "$SCRIPTS on PATH"
      via: "dot_zshrc path array includes $SCRIPTS"
      pattern: "SCRIPTS.*scripts"
    - from: "dot_claude/CLAUDE.md"
      to: "~/.claude/CLAUDE.md"
      via: "chezmoi dot_ prefix mapping"
      pattern: "dot_claude"
---

<objective>
Add the Ralph agent loop script, global CLAUDE.md with Ralph conventions, and Claude Code skills to the dotfiles. Remove the wrong-path dot_config/claude/CLAUDE.md.

Purpose: Make every DevPod container Ralph-ready — the `ralph` command is on PATH, Claude Code auto-reads Ralph conventions, and PRD skills are globally available.
Output: scripts/ralph (executable), dot_claude/CLAUDE.md, dot_claude/skills/ralph/SKILL.md, dot_claude/skills/prd/SKILL.md; dot_config/claude/CLAUDE.md removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ralph-integration/3-RESEARCH.md
@dot_zshrc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ralph script and dot_claude directory with CLAUDE.md and skills</name>
  <files>scripts/ralph, dot_claude/CLAUDE.md, dot_claude/skills/ralph/SKILL.md, dot_claude/skills/prd/SKILL.md</files>
  <read_first>
    - dot_zshrc (confirm $SCRIPTS path variable points to $DOTFILES/scripts)
    - .planning/phases/03-ralph-integration/3-RESEARCH.md (ralph.sh content, CLAUDE.md content, architecture patterns)
  </read_first>
  <action>
Create four files:

**1. `scripts/ralph`** (no .sh extension — directly executable as `ralph` via $SCRIPTS PATH):

Create the Ralph Wiggum agent loop script. Fetch the latest content from `https://raw.githubusercontent.com/snarktank/ralph/main/ralph.sh` and save it as `scripts/ralph`. The script must contain:
- Shebang `#!/bin/bash` and `set -e`
- `--tool` flag parsing (default: amp, accepts amp|claude)
- Max iterations argument parsing (default: 10)
- `SCRIPT_DIR` resolution via `${BASH_SOURCE[0]}`
- Branch change detection and archiving logic using jq to read prd.json branchName
- Progress file initialization
- Main loop that runs the selected tool (amp or claude) with the prompt file
- Completion signal detection via `<promise>COMPLETE</promise>` grep
- 2-second sleep between iterations

After creating the file, run `chmod +x scripts/ralph` to set the executable bit. Chezmoi preserves executable bits from source files.

**2. `dot_claude/CLAUDE.md`** (maps to ~/.claude/CLAUDE.md via chezmoi):

Create with this exact content:

```markdown
# Ralph Agent Instructions

## Your Task
Read the PRD (prd.json) and progress log (progress.txt), then implement the highest-priority
incomplete user story. One story per iteration.

1. Read prd.json to find the highest-priority story where passes=false
2. Read progress.txt for context on what has already been done
3. Checkout or create the feature branch from prd.json branchName
4. Implement the story — one story only
5. Run quality checks: typecheck, lint, tests
6. Commit: "feat: [Story ID] - [Story Title]"
7. APPEND to progress.txt (never replace): date, story ID, files changed, learnings
8. If ALL stories pass: reply <promise>COMPLETE</promise>
9. If stories remain: stop and wait for next iteration

## Progress Report Format
Append to progress.txt (never replace):
- Date and story ID
- What was implemented
- Files changed
- Patterns discovered (add to Codebase Patterns section at top)

## Update CLAUDE.md Files
When editing a directory, add discoverable knowledge to the nearest CLAUDE.md:
API patterns, gotchas, dependencies — not story-specific details.

## Quality Requirements
- All commits must pass typecheck, lint, and tests
- No broken code merges

## Stop Condition
Reply with <promise>COMPLETE</promise> only when ALL stories in prd.json have passes=true.
```

**3. `dot_claude/skills/ralph/SKILL.md`** (maps to ~/.claude/skills/ralph/SKILL.md):

Fetch content from `https://raw.githubusercontent.com/snarktank/ralph/main/skills/ralph/SKILL.md` and save it. This is the Ralph PRD Converter skill that transforms existing PRDs into prd.json format. Key content includes: story sizing rules (one context window per story), dependency ordering (schema -> backend -> UI), verifiable acceptance criteria requirements, JSON output structure with sequential IDs, and pre-conversion archiving checklist.

**4. `dot_claude/skills/prd/SKILL.md`** (maps to ~/.claude/skills/prd/SKILL.md):

Fetch content from `https://raw.githubusercontent.com/snarktank/ralph/main/skills/prd/SKILL.md` and save it. This is the PRD Generator skill with the 3-step workflow: clarifying questions, structured PRD generation, file output. Key content includes: 3-5 lettered-option clarifying questions, 9-section PRD structure, user story format with acceptance criteria, functional requirements numbering, non-goals section, and trigger phrases.
  </action>
  <verify>
    <automated>test -x scripts/ralph && head -1 scripts/ralph | grep -q "bash" && test -f dot_claude/CLAUDE.md && grep -q "prd.json" dot_claude/CLAUDE.md && test -f dot_claude/skills/ralph/SKILL.md && test -f dot_claude/skills/prd/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/ralph exists, is executable (chmod +x), first line is #!/bin/bash
    - scripts/ralph contains the full Ralph Wiggum agent loop (--tool flag, iteration loop, COMPLETE detection, branch archiving)
    - dot_claude/CLAUDE.md exists and contains: "prd.json", "progress.txt", "COMPLETE", "quality checks", all 9 numbered steps
    - dot_claude/skills/ralph/SKILL.md exists and contains PRD-to-JSON conversion instructions
    - dot_claude/skills/prd/SKILL.md exists and contains PRD generator workflow
  </acceptance_criteria>
  <done>All four files exist with correct content; scripts/ralph is executable; dot_claude/ directory structure matches chezmoi mapping to ~/.claude/</done>
</task>

<task type="auto">
  <name>Task 2: Remove wrong-path dot_config/claude/CLAUDE.md</name>
  <files>dot_config/claude/CLAUDE.md</files>
  <read_first>
    - dot_config/claude/CLAUDE.md (confirm it exists and is the wrong-path file)
    - .planning/phases/03-ralph-integration/3-RESEARCH.md (section on wrong CLAUDE.md location)
  </read_first>
  <action>
Delete `dot_config/claude/CLAUDE.md` — this file maps to `~/.config/claude/CLAUDE.md` which Claude Code does NOT read. The correct location is `dot_claude/CLAUDE.md` (maps to `~/.claude/CLAUDE.md`), which was created in Task 1.

Also remove the `dot_config/claude/` directory if it is now empty after deleting CLAUDE.md. Use:
```bash
rm dot_config/claude/CLAUDE.md
rmdir dot_config/claude/ 2>/dev/null || true
```

If the directory contains other files besides CLAUDE.md, leave the directory and only delete CLAUDE.md.
  </action>
  <verify>
    <automated>test ! -f dot_config/claude/CLAUDE.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - dot_config/claude/CLAUDE.md does not exist
    - dot_config/claude/ directory is removed if it was empty after deletion
    - No other files in dot_config/ are affected
  </acceptance_criteria>
  <done>The wrong-path CLAUDE.md is deleted; only dot_claude/CLAUDE.md (correct path) exists in the repo</done>
</task>

</tasks>

<verification>
- `scripts/ralph` is executable and contains the Ralph agent loop
- `dot_claude/CLAUDE.md` exists with Ralph conventions (9-step workflow, quality gates, stop condition)
- `dot_claude/skills/ralph/SKILL.md` and `dot_claude/skills/prd/SKILL.md` exist
- `dot_config/claude/CLAUDE.md` does not exist
- `$SCRIPTS` in dot_zshrc still points to `$DOTFILES/scripts` (unchanged, just confirming)
</verification>

<success_criteria>
- `ralph` script is on PATH via $SCRIPTS and executable
- chezmoi will map dot_claude/CLAUDE.md to ~/.claude/CLAUDE.md (user-scope auto-read location)
- chezmoi will map dot_claude/skills/ to ~/.claude/skills/ (global skills)
- No stale CLAUDE.md at the wrong path (~/.config/claude/CLAUDE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ralph-integration/03-01-SUMMARY.md`
</output>
